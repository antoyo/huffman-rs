initSidebarItems({"fn":[["decode","Decode the huffman-encoded `input` using the Huffman `tree`. The decoding ends when `decompressed_size` is reached."],["decode_with_offset","Decode the huffman-encoded `input` using the Huffman `tree`. The decoding starts at input + `offset`, where `offset` is the number bits to skip (number between 0 and 8). The decoding ends when `decompressed_size` is reached."]],"struct":[["Tree","A huffman tree."]]});